services:
  tablic:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3579:3579"
    volumes:
      - tablic_data:/data
    environment:
      PORT: "3579"
      DB_PATH: "/data/tablic.db"

volumes:
  tablic_data:
